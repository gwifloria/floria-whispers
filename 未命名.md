发现的Claude会话数据结构：

  1. /Users/guihuajiu/.claude/projects/ - 每个项目的会话记录
    - 以项目路径命名的目录，如 -Users-guihuajiu-wonderland-claude-swift
    - 每个会话存储为 .jsonl 文件，包含完整的对话历史
  2. /Users/guihuajiu/.claude/todos/ - 每个会话的待办事项
    - 按会话ID命名的JSON文件，存储结构化的任务列表
  3. /Users/guihuajiu/.claude/shell-snapshots/ - Shell环境快照
    - 保存会话时的shell环境状态，包括变量、别名等
  4. 当前状态对比：
    - 您的项目目前使用简单的进程检测：ps aux | grep claude
    - 实际上Claude维护了更丰富的状态信息，包括项目路径、会话ID、完整对话历史